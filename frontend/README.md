# AI Book Assistant Frontend

Next.js frontend for the AI Book Assistant project.

## Manual Setup

```powershell
cd D:\blockdev\PROJECT\frontend
npm install
npm run dev
```

If PowerShell blocks npm scripts, use:

```powershell
npm.cmd install
npm.cmd run dev
```

## Linting and Formatting

```powershell
npm run lint
npm run format
```
